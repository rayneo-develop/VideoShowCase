<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Relay\nTap Create Group and wait for camera connection"
        android:textSize="14sp"
        android:paddingBottom="4dp" />

    <TextView
        android:id="@+id/tvResolution"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Resolution: —"
        android:textSize="12sp"
        android:paddingBottom="2dp" />

    <TextView
        android:id="@+id/tvFrameRate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Frame rate: —"
        android:textSize="12sp"
        android:paddingBottom="8dp" />

    <Button
        android:id="@+id/btnCreateGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Create Wi-Fi Direct Group"
        android:layout_marginBottom="8dp" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="#000"
        android:minHeight="200dp">

        <SurfaceView
            android:id="@+id/surfaceView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <EditText
        android:id="@+id/etPushUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter stream URL, e.g. rtmp://server/app/stream"
        android:inputType="textUri"
        android:maxLines="1"
        android:textSize="12sp"
        android:padding="8dp"
        android:paddingTop="8dp" />

    <Button
        android:id="@+id/btnStartPush"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start cloud streaming"
        android:layout_marginTop="8dp"
        android:enabled="false" />

</LinearLayout>
